export const createMedia = (
    {
        type,
        url,
        params = {},
        component = null,
        componentProps = {},
        ref = null,
    } ) => (
    {
        type,
        url,
        params,
        component,
        componentProps,
        ref

    })

